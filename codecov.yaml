codecov:
  require_ci_to_pass: false

comment:
  layout: 'header, diff, files, footer'

coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
