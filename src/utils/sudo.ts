// import { chalkError } from '@constants/chalk'
// import chalk from 'chalk'

// export const isRootUser = () => {
//   return process.getuid && process.getuid() === 0
// }

// export const ensureRootUser = () => {
//   if (!isRootUser()) {
//     console.error(chalkError(`Permission required, run the command with ${chalk.underline(chalk.bold('sudo'))} permission`))
//     process.exit(1)
//   }
// }
